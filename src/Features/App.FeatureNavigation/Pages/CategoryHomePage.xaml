<Page
    x:Class="WinUIGallery.Pages.CategoryHomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:WinUIGallery.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="CategoryTitleText"
            Style="{ThemeResource TitleTextBlockStyle}"
            Margin="0,0,0,16" />
        <ListView
            x:Name="ItemsListView"
            Grid.Row="1"
            SelectionMode="Single"
            IsItemClickEnabled="True"
            ItemClick="ItemsListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ControlInfoDataItem">
                    <StackPanel Margin="0,8" Spacing="4">
                        <TextBlock Text="{x:Bind Title}" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                        <TextBlock Text="{x:Bind Subtitle}" Style="{ThemeResource CaptionTextBlockStyle}" TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
